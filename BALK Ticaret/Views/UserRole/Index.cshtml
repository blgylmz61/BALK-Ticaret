@model List<UserRoleViewModel>
@{
    int i = 0;
}
<table class=" table table-striped">
    <thead>
        <tr>
            <th scope="col">S.N.</th>
            <th scope="col">Role</th>
            <th scope="col">Açıklama</th>
           
            <th scope="col">İşlem</th>
        </tr>
    </thead>
    <tbody>
        <form asp-controller="UserRole" asp-action="Create" method="get">
            <button class="btn float-end ms-5 me-5">
                <i class="fa-solid fa-plus fa-xl "></i>
            </button>
        </form>
        @foreach (var item in Model)
        {
            i++;
            <tr>
                <td style="width:2%">@i</td>
                <td style="width:38%">@item.Role</td>
                <td style="width:40%">@(item.Description.Length > 150 ? item.Description.Substring(0, 150) + "..." : item.Description)</td>
                <td style="width:20%; ">
                    <form class="form-icon " asp-action="Create" asp-controller="UserRole" method="get">
                        <input name="Id" value="@item.Id" style="width:none" hidden />
                        <button class="btn  btn-outline-success me-2">
                            <i class="fa-solid fa-plus fa-xs "></i>

                        </button>
                    </form>
                    <form class="form-icon " asp-action="Update" asp-controller="UserRole" method="get">
                        <input name="postId" value="@item.Id" style="width:none" hidden />
                        <button class="btn  btn-outline-primary me-2 ">
                            <i class="fa-regular fa-pen-to-square fa-xs"></i>
                        </button>
                    </form>
                    <form class="form-icon  " asp-action="Delete" asp-controller="UserRole" method="post">
                        <input name="postId" value="@item.Id" style="width:none" hidden />
                        <button class="btn  btn-outline-danger me-2 ">

                            <i class="fa-regular fa-trash-can fa-xs "></i>
                        </button>
                    </form>
                   
                   
            </tr>
        }
    </tbody>
</table>
